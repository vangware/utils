/**
 * Identity function.
 *
 * @category Functions
 * @example
 * ```typescript
 * identity("foo"); // "foo"
 * ```
 * @returns Same value given.
 */
export declare const identity: <const Input>(input: Input) => Input;
